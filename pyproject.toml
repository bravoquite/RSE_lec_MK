[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "volume"
version = "1.1.0"
license = { text = "GPL-3.0-or-later" }
dependencies = ["pint>=0.24"]

[project.optional-dependencies]
test = ["pytest"] 

[tool.setuptools]
py-modules = ["volume"]
